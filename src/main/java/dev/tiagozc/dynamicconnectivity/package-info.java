/**
 * Answers whether there is a path between two objects, but not what the path is
 *
 * <p>We name the objects from 0 to n-1, so 0...n-1 are object names or identifiers
 */
package dev.tiagozc.dynamicconnectivity;
